package de.raindancer.modules.wallsroads.listener;

import org.bukkit.event.Listener;

/** What every listener in this module is, per the project's own structure rule. */
public interface IWallsRoadsListener extends Listener {
}
